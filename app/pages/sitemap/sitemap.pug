extends ../../layouts/default

block head
  - var pageTitle = 'Sitemap'

block content
  +b.sitemap-page
    +header()
      +header-line-menu('карта сайта', 'Добавить заведение', false, 'Личный кабинет')
    +filter()
    +sitemap()
    +e.spacer
    +footer()
    +popups()
