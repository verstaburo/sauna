extends ../../layouts/default

block head
  - var pageTitle = 'Main'

block content
  +b.index
    +header()
    +topline-menu('карта сайта', 'sitemap.html', 'Добавить заведение').__topline
    +put-block-into-slot('headblock', 'xl')
      +headblock-main()._main
    +filter()
    +tags()
    +mapblock('Показать заведения рядом').__mapblock
    +put-block-into-slot('headblock', 'md')
    +places()
    +types()
    +random()
    +offers()
    +imgline()
    +places-new()
    +desc()
    +e.banner
    +reviews()
    +blog()
    +lastview()
    +footer()
    +popups()
