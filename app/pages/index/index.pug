extends ../../layouts/default

block head
  - var pageTitle = 'Main'

block content
  +b.index
    +header()
      +header-line-menu('карта сайта', 'Добавить заведение')
    +put-block-into-slot('headblock', 'xl')
      +headblock-main()._main
    +filter()
    +tags()
    +mapblock('Показать заведения рядом').index__mapblock
    +put-block-into-slot('headblock', 'md')
    +places()
    +types()
    +random()
    +offers()
    +imgline()
    +places-new()
    +desc()
    +e.banner
    +reviews()
    +blog()
    +lastview()
    +footer()
    +popups()
