mixin breadcrumbs(json)
  +b.UL.breadcrumbs(itemscope itemtype='http://schema.org/BreadcrumbList')&attributes(attributes)
    each crumb in json
      +e.LI.item(itemprop='itemListElement')
        +link(href='#')
          =crumb
